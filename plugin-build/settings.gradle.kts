pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
    }
}

rootProject.name = ("com.tkormachev.kotlin.gradle.qa.reporting.plugin")

include(":plugin")
