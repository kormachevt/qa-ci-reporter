pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        jcenter()
    }
}

rootProject.name = ("com.github.kormachevt.qa.ci.reporter.plugin")

include(":plugin")
